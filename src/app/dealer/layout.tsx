export const metadata = {
  title: 'GIO RAPTOR | Dealing Desk',
  description: 'Institutional dealing desk operating system',
};

export default function DealerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
